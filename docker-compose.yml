version: '3.8'

# =============================================================================
# Composable Toolkit - Full Stack Docker Compose
# All 19 backend services + frontend + infrastructure
# =============================================================================

networks:
  toolkit-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
  kafka-data:
  zookeeper-data:
  timescale-data:
  prometheus-data:
  grafana-data:

services:
  # ===========================================================================
  # Infrastructure Services
  # ===========================================================================

  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: toolkit-postgres
    environment:
      POSTGRES_USER: toolkit
      POSTGRES_PASSWORD: toolkit_dev
      POSTGRES_DB: toolkit
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - toolkit-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U toolkit"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: toolkit-redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - toolkit-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # Kafka
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: toolkit-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    volumes:
      - zookeeper-data:/var/lib/zookeeper/data
    networks:
      - toolkit-network

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: toolkit-kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    volumes:
      - kafka-data:/var/lib/kafka/data
    networks:
      - toolkit-network
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 30s
      timeout: 10s
      retries: 5

  # TimescaleDB (for time-series data)
  timescale:
    image: timescale/timescaledb:latest-pg15
    container_name: toolkit-timescale
    environment:
      POSTGRES_USER: toolkit
      POSTGRES_PASSWORD: toolkit_dev
      POSTGRES_DB: timeseries
    ports:
      - "5433:5432"
    volumes:
      - timescale-data:/var/lib/postgresql/data
    networks:
      - toolkit-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U toolkit"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ===========================================================================
  # Backend Services (01-19)
  # ===========================================================================

  # App 01: E-Commerce Inventory
  app01-inventory:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app01
    command: python -m uvicorn examples.01_ecommerce_inventory.main:app --host 0.0.0.0 --port 8001 --reload
    ports:
      - "8001:8001"
    environment:
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
      REDIS_URL: redis://redis:6379/0
    depends_on:
      - postgres
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # App 02: Real-Time Analytics
  app02-analytics:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app02
    command: python -m uvicorn examples.02_realtime_analytics_dashboard.main:app --host 0.0.0.0 --port 8002 --reload
    ports:
      - "8002:8002"
    environment:
      KAFKA_BROKERS: kafka:9092
      REDIS_URL: redis://redis:6379/1
      TIMESCALE_URL: postgresql://toolkit:toolkit_dev@timescale:5432/timeseries
    depends_on:
      - kafka
      - redis
      - timescale
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 03: File Processing
  app03-file-processor:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app03
    command: python -m uvicorn examples.03_file_processing_service.main:app --host 0.0.0.0 --port 8003 --reload
    ports:
      - "8003:8003"
    environment:
      REDIS_URL: redis://redis:6379/2
    depends_on:
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
      - ./uploads:/app/uploads
    networks:
      - toolkit-network

  # App 04: API Gateway
  app04-gateway:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app04
    command: python -m uvicorn examples.04_microservices_gateway.main:app --host 0.0.0.0 --port 8004 --reload
    ports:
      - "8004:8004"
    environment:
      REDIS_URL: redis://redis:6379/3
    depends_on:
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 05: Data Export
  app05-export:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app05
    command: python -m uvicorn examples.05_data_export_service.main:app --host 0.0.0.0 --port 8005 --reload
    ports:
      - "8005:8005"
    environment:
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
    depends_on:
      - postgres
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 06: Kappa Architecture Monitor
  app06-kappa:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app06
    command: python -m uvicorn examples.06_kappa_architecture_monitor.main:app --host 0.0.0.0 --port 8006 --reload
    ports:
      - "8006:8006"
    environment:
      KAFKA_BROKERS: kafka:9092
    depends_on:
      - kafka
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 07: Event Sourcing
  app07-eventsourcing:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app07
    command: python -m uvicorn examples.07_event_sourcing.main:app --host 0.0.0.0 --port 8007 --reload
    ports:
      - "8007:8007"
    environment:
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
    depends_on:
      - postgres
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 08: TimeSeries Database
  app08-timeseries:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app08
    command: python -m uvicorn examples.08_timeseries_database.main:app --host 0.0.0.0 --port 8008 --reload
    ports:
      - "8008:8008"
    environment:
      TIMESCALE_URL: postgresql://toolkit:toolkit_dev@timescale:5432/timeseries
    depends_on:
      - timescale
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 09: Distributed Cache
  app09-cache:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app09
    command: python -m uvicorn examples.09_distributed_cache.main:app --host 0.0.0.0 --port 8009 --reload
    ports:
      - "8009:8009"
    environment:
      REDIS_URL: redis://redis:6379/4
    depends_on:
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 10: Message Queue Manager
  app10-queue:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app10
    command: python -m uvicorn examples.10_message_queue_manager.main:app --host 0.0.0.0 --port 8010 --reload
    ports:
      - "8010:8010"
    environment:
      KAFKA_BROKERS: kafka:9092
    depends_on:
      - kafka
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 11: Rate Limiter Dashboard
  app11-ratelimiter:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app11
    command: python -m uvicorn examples.11_distributed_rate_limiter.main:app --host 0.0.0.0 --port 8011 --reload
    ports:
      - "8011:8011"
    environment:
      REDIS_URL: redis://redis:6379/5
    depends_on:
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 12: Lambda Architecture
  app12-lambda:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app12
    command: python -m uvicorn examples.12_lambda_architecture.main:app --host 0.0.0.0 --port 8012 --reload
    ports:
      - "8012:8012"
    environment:
      KAFKA_BROKERS: kafka:9092
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
    depends_on:
      - kafka
      - postgres
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 13: CDC Monitor
  app13-cdc:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app13
    command: python -m uvicorn examples.13_cdc_pipeline.main:app --host 0.0.0.0 --port 8013 --reload
    ports:
      - "8013:8013"
    environment:
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
      KAFKA_BROKERS: kafka:9092
    depends_on:
      - postgres
      - kafka
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 14: Recommendation Engine
  app14-recommendations:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app14
    command: python -m uvicorn examples.14_recommendation_engine.main:app --host 0.0.0.0 --port 8014 --reload
    ports:
      - "8014:8014"
    environment:
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
      REDIS_URL: redis://redis:6379/6
    depends_on:
      - postgres
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 15: Search Engine
  app15-search:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app15
    command: python -m uvicorn examples.15_search_engine.main:app --host 0.0.0.0 --port 8015 --reload
    ports:
      - "8015:8015"
    environment:
      REDIS_URL: redis://redis:6379/7
    depends_on:
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 16: Feature Store
  app16-featurestore:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app16
    command: python -m uvicorn examples.16_feature_store.main:app --host 0.0.0.0 --port 8016 --reload
    ports:
      - "8016:8016"
    environment:
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
      REDIS_URL: redis://redis:6379/8
    depends_on:
      - postgres
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 17: OLAP Dashboard
  app17-olap:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app17
    command: python -m uvicorn examples.17_realtime_olap.main:app --host 0.0.0.0 --port 8017 --reload
    ports:
      - "8017:8017"
    environment:
      DATABASE_URL: postgresql://toolkit:toolkit_dev@postgres:5432/toolkit
    depends_on:
      - postgres
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 18: Distributed Tracing
  app18-tracing:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app18
    command: python -m uvicorn examples.18_distributed_tracing.main:app --host 0.0.0.0 --port 8018 --reload
    ports:
      - "8018:8018"
    environment:
      REDIS_URL: redis://redis:6379/9
    depends_on:
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # App 19: Probabilistic Structures
  app19-probabilistic:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-app19
    command: python -m uvicorn examples.19_probabilistic_structures.main:app --host 0.0.0.0 --port 8019 --reload
    ports:
      - "8019:8019"
    environment:
      REDIS_URL: redis://redis:6379/10
    depends_on:
      - redis
    volumes:
      - ./src:/app/src
      - ./examples:/app/examples
    networks:
      - toolkit-network

  # ===========================================================================
  # Frontend Services
  # ===========================================================================

  # Frontend - Development
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-frontend
    ports:
      - "3000:3000"
    environment:
      VITE_API_BASE_URL: http://localhost:8001
    volumes:
      - ./frontend:/app
      - /app/node_modules
    networks:
      - toolkit-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ===========================================================================
  # Documentation Site
  # ===========================================================================

  docs:
    build:
      context: ./docs
      dockerfile: Dockerfile
      target: development
    container_name: toolkit-docs
    ports:
      - "5173:5173"
    volumes:
      - ./docs:/app
      - /app/node_modules
    networks:
      - toolkit-network
    command: npm run docs:dev -- --host 0.0.0.0

  # ===========================================================================
  # Observability Stack
  # ===========================================================================

  # Prometheus - Metrics collection
  prometheus:
    image: prom/prometheus:latest
    container_name: toolkit-prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
    ports:
      - "9090:9090"
    volumes:
      - ./observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - ./observability/prometheus/rules:/etc/prometheus/rules
      - prometheus-data:/prometheus
    networks:
      - toolkit-network
    restart: unless-stopped

  # Grafana - Metrics visualization
  grafana:
    image: grafana/grafana:latest
    container_name: toolkit-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_INSTALL_PLUGINS=grafana-piechart-panel
    volumes:
      - ./observability/grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./observability/grafana/datasources:/etc/grafana/provisioning/datasources
      - grafana-data:/var/lib/grafana
    networks:
      - toolkit-network
    depends_on:
      - prometheus
    restart: unless-stopped

  # Jaeger - Distributed tracing
  jaeger:
    image: jaegertracing/all-in-one:latest
    container_name: toolkit-jaeger
    ports:
      - "5775:5775/udp"
      - "6831:6831/udp"
      - "6832:6832/udp"
      - "5778:5778"
      - "16686:16686"  # UI
      - "14268:14268"
      - "14250:14250"
      - "9411:9411"
    environment:
      - COLLECTOR_ZIPKIN_HOST_PORT=:9411
    networks:
      - toolkit-network
    restart: unless-stopped

  # Loki - Log aggregation
  loki:
    image: grafana/loki:latest
    container_name: toolkit-loki
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - toolkit-network
    restart: unless-stopped

  # Promtail - Log shipper
  promtail:
    image: grafana/promtail:latest
    container_name: toolkit-promtail
    volumes:
      - /var/log:/var/log
      - ./observability/promtail/config.yml:/etc/promtail/config.yml
    command: -config.file=/etc/promtail/config.yml
    networks:
      - toolkit-network
    depends_on:
      - loki
    restart: unless-stopped
